import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.personalfinance.assistant',
  appName: 'Personal Finance Assistant',
  webDir: 'dist'
};

export default config;
